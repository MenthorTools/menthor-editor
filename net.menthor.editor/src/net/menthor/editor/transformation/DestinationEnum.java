package net.menthor.editor.transformation;

public enum DestinationEnum {
	APP, /** open the result into another application */
	TAB, /** visualize the result in Menthor's embedded text editor */
	FILE /** write the result in a file */
}
